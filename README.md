# freshpops
GitHub Pages project for FRESH POPS.

## Deploy
1. Create repo (example): `kellsnollenberger/freshpops`
2. Push `main` branch.
3. In GitHub Pages, set source to `Deploy from branch` -> `main` / root.
4. Ensure DNS for `freshpops.nyc` points to GitHub Pages.
